<div class='messenger'>
	{{#if selectedChat}}
		<div>{{{header}}}</div>
		<div class='messenger__messages'>
			{{#each messages}}
				{{{this}}}
			{{/each}}
		</div>
		<div class='messenger__footer'>
			{{{input}}}
			{{{button}}}
		</div>
	{{else}}
		Выбирете чат
	{{/if}}
</div>